const app = require('./config/server');
const data = require('./config/firebaseConfig'); 
//const passport = require('./config/passport');


//to do some thing !
module.exports = app;
module.exports = data;
//module.exports = passport;